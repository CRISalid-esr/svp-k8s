apiVersion: v1
kind: Secret
metadata:
  name: sovisuplus-secret
data:
  db_host: "${SVP_DB_HOST}"
  db_port: "${SVP_DB_PORT}"
  db_name: "${SVP_DB_NAME}"
  db_user: "${SVP_DB_USER}"
  db_password: "${SVP_DB_PASSWORD}"
  nextauth_secret: "${NEXTAUTH_SECRET}"
  keycloak_client_secret: "${KEYCLOAK_CLIENT_SECRET}"